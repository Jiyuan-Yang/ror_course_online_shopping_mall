<% provide(:title, 'Shop') %>
<h1><%= @shop.name %></h1>
<div><%= @shop.description %></div>
<h2>products</h2>
<% if @shop.products.count == 0 %>
  <div>
    it seems that you don't have any products,
    <%= link_to "click here", create_product_get_path %> to create you product!
  </div>
<% else %>
  <% @shop.products.each do |item| %>
    <div><%= item.id %>|<%= link_to item.name, product_path(item.id) %>|
      <%= item.description %>|<%= item.price %></div>
  <% end %>
  <div><%= link_to "click here", create_product_get_path %> to create you product!</div>
<% end %>