<% provide(:title, '店铺列表') %>
<h1><%= @user.name + '的店铺列表' %></h1>
<% if @user.shops.count == 0 %>
  <div class="jumbotron">
    <h1>Oops!</h1>
    <p>您好像还没有店铺。</p>
    <p>在the Max Jim's，只要您获得了备案许可，在遵守各项法律法规的前提下，您可以自由创建自己的店铺。</p>
    <p>点击下面的按钮，即刻创建！</p>
    <br/>
    <br/>
    <br/>
    <div class="text-center">
      <%= link_to '<button class="btn btn-primary btn-lg">创建店铺</button>'.html_safe,
                  create_shop_get_path %>
    </div>
  </div>
<% else %>
  <div class="container-fluid">
    <table class="table table-striped">
      <tr>
        <th class="col-md-2">店铺ID</th>
        <th class="col-md-4">店铺名称</th>
        <th class="col-md-4">店铺描述</th>
        <th class="col-md-2">操作</th>
      </tr>
      <%= render @shops %>
    </table>
  </div>
  <%= will_paginate @shops %>

  <div class="text-center">
    <%= link_to '<button class="btn btn-primary">创建店铺</button>'.html_safe, create_shop_get_path %>
  </div>
<% end %>