<% User.all.each do |item| %>
  <div><%= item.id %>|<%= item.name %>|<%= item.character %>|<%= link_to 'settings', edit_user_path(item.id) %>
  |<%= link_to 'delete', user_path(item.id), method: :delete, data: {confirm: 'Are you sure'} %></div>
<% end %>